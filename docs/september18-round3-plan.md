# September 18 third-round declaration

The user authorizes autonomous work until an official public EMD beats **37.62858** or today's remaining submissions are exhausted. The last verified state is three of five used, with two remaining; refresh official status before any upload. CSV acceptance is not confirmation of prize eligibility. The hosted Astra alias is not an immutable checkpoint, identical fresh predictions are not guaranteed, and accessibility/organizer acceptance of this model arrangement remains unverified. The user was informed and explicitly renewed the experiment goal; do not contact organizers or purchase additional service capacity.

## Two fixed hypotheses

1. **Equal VLM/native-mass blend.** Average the original visual-language CDF and saved native square-root mass ridge CDF at exactly 50/50. Align all 24 held-out predictions and ten test predictions by soil ID. Both components already exclude the held-out soil. Recompute original EMD and per-soil gains against both components; no weight selection, fitted calibration or second-stage training. The observed residual correlation is about 0.73, so complementarity is plausible but limited. Camera-transfer evidence is unavailable for the VLM component.
2. **Similarity-selected visual-language examples.** Retain the original model, low reasoning, exact task instructions, eight examples, calibrated 100 mm image preparation, and one example photo / one query photo per available camera. Replace random exemplar selection with the eight nearest training soils under squared Euclidean distance on the existing 23 native spectral features. Average photo features within each soil. For each held-out query, fit feature means and population standard deviations on the other 23 training soils only (zero standard deviation becomes one); final test queries use all 24 training soils. Exclude the query ID before fitting or selecting. Sort available training IDs alphabetically and break equal-distance ties by that order. Present the eight examples in increasing-distance order. Use no labels to choose examples or distances, and do not remove unusual soils.

The similarity test changes the exemplar-selection policy only. Its preprocessing and model settings otherwise match the saved VLM. Original random examples reach only 14 of 24 training soils across the previous validation folds; retrieval may supply more relevant examples but could narrow their diversity. There is no nearest-neighbor-count search, prompt search, label-based exemplar selection, or test-ID identification.

## Model requests and validation

Use fresh anonymous, tool-disabled ephemeral Codex CLI contexts, reusing the original isolation and response validation. Maximum **34 new requests** (24 whole-soil holdouts plus ten test predictions), **1,000,000 aggregate input tokens**, and **50,000 aggregate output tokens including reasoning**. Dispatch sequentially, check consumed usage before each request, and retain the 2,048-character final-text ceiling. This is an aggregate stopping budget, not a verified per-request server-side token cap. Existing authenticated included capacity only; no reset, purchase, or paid API setup.

Commit the declaration before evaluation and the reviewed, tested producing code before real model requests. Save every prompt, image, raw response, event log, selected exemplar ID, distance and training-scaler population outside the model context. Check source hashes, client version and completed-response integrity before resuming. Never automatically retry an uncertain/failed request, repair malformed curves or drop failed holdouts. All 24 validation and ten test responses must be valid for this candidate to be eligible. If the retrieval candidate cannot complete under these limits, record the failure and use the already saved native square-root mass ridge as a predeclared fallback; no replacement model search.

## Submission and preservation

Freeze the two eligible candidate CSVs and their upload order before the first new public result. Order by ascending whole-soil local EMD, with candidate name breaking ties. A weaker local candidate remains eligible as an informative fixed hypothesis given the observed local/public mismatch. Do not change predictions, exemplars, weights or order from this round's public results. Stop immediately on a new best or confirmed allowance exhaustion.

Use a new `artifacts/experiments/september18_round3/` directory and preserve every earlier artifact byte-for-byte. Validate template order, ranges, monotonicity, duplicate submissions, source/output hashes, quota, accepted references and successful producing-code CI. Pin the frozen selection hash in the one-upload helper and record a request receipt before calling Kaggle. Resolve uncertain requests before any further upload. Save results, merge tested commits to `main`, push with `will.murray2@icloud.com`, and remove the fully merged task branch.
