competition: soil-grain-size-from-photos

paths:
  raw_dir: data/raw/latest
  curated_dir: data/curated/latest
  artifacts_dir: artifacts
  reports_dir: artifacts/reports
  submissions_dir: artifacts/submissions

files:
  train: train.csv
  test: test.csv
  sample_submission: sample_submission.csv
  ppm: ppm.csv

photo_dirs:
  training: All_Photos_training
  test: All_Photos_test
